/*
package types contains low level types used by other packages
This package should have no dependencies outside the standard library
and should contain nothing but type  definitions.
*/
package types
